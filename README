Pileup is a simple thread-safe binary heap implementation for Common Lisp,
under MIT-style licence.

TODO

* HEAP-DELETE should accept keyword arguments similar to CL:DELETE.
* Add HEAP-DELETE-IF and HEAP-DELETE-IF-NOT.
* MAKE-HEAP should also accept :ELEMENT-TYPE.
  Unboxed allocation for long-lived heaps storing numbers.
* Add other types of heaps.
